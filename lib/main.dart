import 'package:flutter/material.dart';
import 'Splahscreen.dart';

void main() => runApp(new MaterialApp(
  theme:
  ThemeData(accentColor: Colors.white, primaryColor: Colors.blueGrey),
  debugShowCheckedModeBanner: false,
  home: SplashScreen(),
));
